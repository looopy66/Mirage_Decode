name: Build Android APK (Dependency Order Fixed)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Install Project Dependencies
        working-directory: ./web
        run: |
          yarn install --ignore-engines
          # 【关键修正】在构建前安装 Capacitor 插件，否则 Vite 无法解析 import
          yarn add @capacitor/core @capacitor/filesystem --ignore-engines
          yarn add -D @capacitor/cli @capacitor/android --ignore-engines

      - name: Patch Frontend Code for Mobile Storage
        working-directory: ./web/src
        run: |
          node -e "
          const fs = require('fs');
          const path = 'App.tsx';
          if (!fs.existsSync(path)) process.exit(0);
          let content = fs.readFileSync(path, 'utf8');

          const nativeSaveFn = \`
          import { Filesystem, Directory } from '@capacitor/filesystem';

          /** @ts-ignore */
          const safeDownloadBase64 = async (base64Data: string, filename: string) => {
            try {
              const base64Content = base64Data.includes(',') ? base64Data.split(',')[1] : base64Data;
              await Filesystem.writeFile({
                path: 'Mirage_' + Date.now() + '.png',
                data: base64Content,
                directory: Directory.Documents,
                recursive: true
              });
              alert('图片已保存至系统【文档(Documents)】文件夹');
            } catch (e) {
              console.error('Save failed', e);
              const link = document.createElement('a');
              link.href = base64Data;
              link.download = filename;
              document.body.appendChild(link);
              link.click();
              document.body.removeChild(link);
            }
          };\n\`;

          content = nativeSaveFn + content;
          content = content.replace(/saveAs\((.*?)\)/g, 'safeDownloadBase64($1, \"mirage.png\")');
          fs.writeFileSync(path, content);
          "

      - name: Build Web Project
        working-directory: ./web
        run: npx vite build

      - name: Initialize Capacitor and Android
        working-directory: ./web
        run: |
          # 路径修正：处理原项目 output 路径偏移
          if [ -d "../docs" ]; then
            mv ../docs dist
          fi
          
          npx cap init "MirageDecode" "com.tankfactory.mirage" --web-dir dist
          npx cap add android
          npx cap sync android

      - name: Inject Android Permissions
        working-directory: ./web/android/app/src/main
        run: |
          node -e "
          const fs = require('fs');
          const file = 'AndroidManifest.xml';
          let content = fs.readFileSync(file, 'utf8');
          
          const perms = \`
          <uses-permission android:name=\"android.permission.READ_EXTERNAL_STORAGE\" />
          <uses-permission android:name=\"android.permission.WRITE_EXTERNAL_STORAGE\" />
          <uses-permission android:name=\"android.permission.MANAGE_EXTERNAL_STORAGE\" />
                \`;
                
                content = content.replace('<application', perms + '    <application android:requestLegacyExternalStorage=\"true\" ');
                fs.writeFileSync(file, content);
                "

      - name: Build APK with Gradle
        working-directory: ./web/android
        run: |
          chmod +x ./gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Mirage_Mobile_Fixed_APK
          path: web/android/app/build/outputs/apk/debug/app-debug.apk
